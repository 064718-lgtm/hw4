streamlit>=1.32.0
# Torch 2.5.1 is available on Streamlit Cloud's Python runtime; pin to avoid install failures.
torch==2.5.1
# Keep diffusers/transformers moderate to reduce cold-start time while remaining compatible.
diffusers==0.29.2
transformers==4.44.2
accelerate>=0.25.0
safetensors>=0.4.2
Pillow>=10.2.0
